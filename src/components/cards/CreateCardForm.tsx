import React from "react";
import {
  Box,
  Stack,
  Typography,
  FormLabel,
  TextField,
  FormControl,
  Select,
  MenuItem,
} from "@mui/material";

const CreateCardForm = ({}) => {
  return (
    <div>
      <form></form>
    </div>
  );
};

export default CreateCardForm;
